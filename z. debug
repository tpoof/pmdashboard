Continuing the investigation: main.tpl and main_iframe.tpl (in both LEAF_Request_Portal and LEAF_Nexus) are confirmed clean — main_iframe.tpl has no header/menu/login includes and doesn't reference $menu/$login. So the duplicate header seen when loading these two actions inside a modal iframe must be coming from {$body} itself — the report/form body template rendered for these specific actions, not the shared layout.

The two actions to trace:
- a=LEAF_Start_Request (used for both the Help Library "Request Support" form, id=form_ba7de, and the Learning Center "Training Registration" form, id=form_a3df9 — same action, different form ids, so check whether behavior differs by id or is shared across all LEAF_Start_Request calls)
- a=spotlight_direct

Please:
1. Trace exactly which template(s) get rendered into $body for a=LEAF_Start_Request and a=spotlight_direct in LEAF_Request_Portal/report.php — find the code path from the $_GET['a'] switch/dispatch to the actual .tpl file(s) assigned as body content.
2. Show me the full contents of whatever body template(s) you find. I'm looking for a top-of-page block that visually mimics a global header/nav — search for ids/classes like header, menu, nav, headerMenu, menu2, logo, or any hardcoded HTML resembling a site header (title bar, nav links, login/user info) sitting inside what should just be form/report content.
3. Confirm whether this block is present unconditionally, or whether it's already gated behind some flag (and if gated, what flag, and why it's still showing — e.g. wrong variable name, inverted condition, or the flag never actually gets passed down to this specific template).
4. Check whether there's an existing $isIframe (or equivalently named) variable used elsewhere in the app for exactly this purpose — reuse that name/pattern if it exists, don't invent a new one.
5. Propose the minimal fix: wrapping the offending block in {if !$isIframe} (or the existing equivalent), plus what needs to happen upstream (report.php or a shared form-rendering function) to actually assign that variable when $_GET['iframe'] is set and pass it into these specific body templates' context, if it isn't reaching them today.

Still no edits — show me the template block and the proposed {if} wrap so I can review before anything changes.