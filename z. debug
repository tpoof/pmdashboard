You found this ?a= resolver pattern in LEAF_Nexus/report.php and LEAF_Request_Portal/report.php: a switch for a few special cases, falling back to file_exists("templates/reports/{$action}.tpl").

Search across all repos in the LEAF GitHub org for a repo that corresponds to the "Launchpad" application. Look for:

Any repo whose name, README, or file paths reference "Launchpad" or "launchpad."
Within candidate repos, a report.php (or similarly-named front controller) that follows the same switch + file_exists("templates/reports/{$action}.tpl") pattern you found in the other two apps.
A templates/reports/ directory (or equivalent) containing files with an lp_ prefix — e.g. lp_impact.tpl, lp_help_library.tpl, lp_find_site.tpl — confirming this is the right app.

If you find it: confirm whether it uses the identical resolver pattern, and whether adding an alias (a second template file that reuses an existing one's content, or a new switch case) would work the same way it would in the two apps you already checked.

If no repo matches — no "Launchpad"-named repo, or none containing lp_-prefixed templates — say so explicitly. Don't assume it uses the same pattern as LEAF_Nexus/LEAF_Request_Portal just because those two do; confirm it directly or report that you couldn't locate it.