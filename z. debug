This is the targeted follow-up to close out the investigation definitively. Please fetch and show full contents (not summaries, not capped/truncated results) for each of the following, with file paths and line numbers:

1. The full definition of customTemplate() — wherever it's declared (likely a shared utility/include under LEAF_Request_Portal or a common lib shared across LEAF_* apps). Show me exactly how it resolves a template name string into an actual file path: does it check a site config, environment variable (prod/preprod/vapo), per-installation theme/override directory, or database-stored template mapping before falling back to the literal filesystem path?

2. Based on what customTemplate() does, tell me the ACTUAL file path it resolves to for main_iframe.tpl in this environment/deployment — not the base templates/main_iframe.tpl we already confirmed is clean, but whatever override (if any) actually gets loaded at runtime. Fetch and show that file's full contents if it differs from the base file.

3. Trace exactly how a=spotlight_direct is dispatched in report.php, since templates/reports/spotlight_direct.tpl does not exist as a literal file. Is the action name transformed/mapped before being passed to fetch("reports/{$action}.tpl")? Is there a plugin system, a database-backed template registry, or a different dispatch branch entirely for this action? Show me the exact code path from $_GET['a'] = 'spotlight_direct' to whatever template content actually ends up in $body.

4. Show me the full include/bootstrap chain that runs in report.php BEFORE $main->display() is called — every require/include at the top of the file and anything they in turn load — looking specifically for anything that unconditionally emits header/nav HTML or loads a header-rendering script, independent of the main.tpl vs main_iframe.tpl choice.

5. Once you've traced the actual resolved template(s) for both actions in this environment, quote the exact header/nav-like markup block you find — file, line numbers, and the literal HTML/Smarty — so we finally have the real offending code.

6. Only after that: propose the minimal diff (file + line-anchored) to gate that specific block behind the existing isIframe convention, plus whatever upstream change is needed to make sure $isIframe actually reaches that specific resolved template's rendering context.

Still no edits — this is the trace that should finally locate the real source, given the previous two passes ruled out the base templates.