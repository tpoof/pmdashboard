Final pass on this investigation. We've confirmed:
- report.php assigns body via: $main->assign('body', $t_form->fetch("reports/{$action}.tpl"));
- So a=LEAF_Start_Request renders LEAF_Request_Portal/templates/reports/LEAF_Start_Request.tpl
- a=spotlight_direct renders LEAF_Request_Portal/templates/reports/spotlight_direct.tpl
- main_iframe.tpl renders {$body} directly with no wrapping chrome, so any header-like markup seen in the modal has to live inside these two report templates or something they {include}.
- The existing convention in this repo for this exact purpose is a variable named isIframe (seen used elsewhere in templates already) — reuse that name, don't invent a new one.

Please, this time actually fetch and show me in full:
1. templates/reports/LEAF_Start_Request.tpl — complete contents.
2. templates/reports/spotlight_direct.tpl — complete contents.
3. Any {include file="..."} statements inside either of those two templates — fetch and show those included files too, recursively, until there are no more includes left to follow. Don't stop at one level.
4. Within all of that, once you have full contents (not truncated/capped results), point to the exact top-of-body block that renders a header/nav-like element — the visual header showing in the modal. Quote the exact lines, with file name and line numbers.
5. Confirm whether isIframe is already assigned anywhere upstream of these two templates (in report.php, a shared form-rendering helper, or a base class), or whether it needs to be newly assigned via: $t_form->assign('isIframe', isset($_GET['iframe']) ? 1 : 0); before the fetch() call for these actions.
6. Give me the final proposed diff: the exact {if !$isIframe} ... {/if} wrap around the offending block (with file + line numbers), plus the exact report.php change (if needed) to assign isIframe before fetching these two templates. Still no edits applied — just the precise proposed diff for me to review and apply myself.