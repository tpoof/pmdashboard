I'm embedding a LEAF form inside an
<iframe>
  on a different LEAF-hosted page (a modal on the Launchpad). The iframe's src
  is:
  https://leaf.va.gov/platform/support/report.php?a=LEAF_Start_Request&id=form_ba7de&title=Consultation+Request&iframe=1
  When loaded inside the iframe, the form renders its own full site header/nav
  in addition to the parent page's header — I get two stacked headers. I added
  &iframe=1 as a query param hoping it would suppress the form's own header/nav
  when embedded, but it isn't doing anything. Please find, in report.php (and
  whatever controller/template it dispatches to for a=LEAF_Start_Request), the
  actual mechanism that determines whether the page's header/nav is rendered: Is
  there an existing query param, header check, or session flag that suppresses
  the header for embedded/iframe contexts? What's it actually called? If nothing
  like that exists today, where's the right place to add one (e.g. a Smarty
  template conditional around the header include), and what would the minimal
  change look like? Is there a reason header suppression might intentionally not
  be supported for this route (e.g. security/framing policy), that I should know
  about before trying to add it?</iframe
>
