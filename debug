Request: New Entra ID App Registration for LEAF Outlook Calendar Sync Register a
new App Registration (do not reuse LEAF's existing login app registration) —
type: Single-page application (SPA), public client, no client secret. Redirect
URI: https://leaf.va.gov/platform/calendar (SPA platform type). API permissions
(delegated, not application): Calendars.ReadWrite.Shared User.Read
offline_access Grant admin consent for those permissions (tenant-wide, so
individual users aren't prompted to consent themselves). Confirm cloud
environment: is this tenant commercial Azure AD, GCC, or GCC High? (Determines
whether Graph calls use graph.microsoft.com or graph.microsoft.us.) Send back:
Application (client) ID, Directory (tenant) ID, and the cloud environment
confirmed in #5. Separately (likely Exchange/M365 admin, may be same person):
grant Full Access delegate permission on the shared mailbox LEAF@va.gov to
whichever users/group should see and sync this calendar.
