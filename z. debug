Continuing from the previous investigation: report.php in LEAF_Request_Portal correctly branches on $_GET['iframe'] and renders main_iframe.tpl instead of main.tpl when the param is present. But the header/nav is still visibly rendering even when main_iframe.tpl is used — so the bug is inside main_iframe.tpl itself, not in report.php's routing.

Please:
1. Show me the full contents of main_iframe.tpl (LEAF_Request_Portal) and compare it side-by-side against main.tpl from the same directory — specifically, identify every {include} or template block related to header, nav, menu, or login that appears in main.tpl but should be absent (or conditionally excluded) from main_iframe.tpl.
2. Check whether main_iframe.tpl includes a shared partial (e.g. header.tpl, nav.tpl, menu.tpl) that itself doesn't check for iframe context — i.e., main_iframe.tpl might correctly skip an include that main.tpl has, but if header/nav markup lives in something ALSO included by main_iframe.tpl (like a shared layout wrapper both templates extend), that would explain the leak.
3. Check whether the $menu / $login template variables (confirmed assigned unconditionally in report.php before template selection, per the earlier findings) are used inside main_iframe.tpl to render header/nav markup even though the template is supposed to be the "no chrome" version — if so, that's likely the actual bug: the variables are present so the template renders them regardless of context.
4. If you find the leak, propose the minimal fix to main_iframe.tpl (or the shared partial it includes) to exclude header/nav rendering, while preserving whatever body content, forms, and scripts are required for the embedded report/form to function.
5. Also check LEAF_Nexus/report.php and its equivalent main_iframe.tpl (mentioned as using the same iframe pattern) — confirm whether it has the same bug or if it correctly suppresses header/nav, since that would tell us whether this is a LEAF_Request_Portal-specific regression or a platform-wide template issue.

Don't make changes yet — show me the template diffs and your proposed fix location first.