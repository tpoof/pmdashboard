This investigation has hit a contradiction and needs a different angle. Confirmed so far:
- report.php branches correctly: isset($_GET['iframe']) selects main_iframe.tpl over main.tpl.
- main_iframe.tpl (LEAF_Request_Portal) is clean — no header/menu/login/footer markup, no includes.
- LEAF_Start_Request.tpl (the body template for a=LEAF_Start_Request) is also clean — no header/nav-like markup at all.
- spotlight_direct.tpl doesn't exist at templates/reports/spotlight_direct.tpl — that action must resolve somewhere else.

Yet loading either URL directly in a browser (with &iframe=1 present) still visibly shows a full header/nav. Since both templates in the actual render path are clean, the header must be coming from somewhere outside main.tpl/main_iframe.tpl/LEAF_Start_Request.tpl entirely. Two theories to check:

THEORY 1 — customTemplate() resolves to something other than what we've been reading.
1. Find the definition of customTemplate() (referenced as customTemplate('main.tpl') in report.php). Show its full source.
2. Determine what it actually does: does it ever return a different file path than the literal string passed in, based on site config, environment (prod/preprod/vapo), theme, or per-installation overrides? If so, show me how to determine which actual file gets loaded for this specific environment/site, and fetch that file's real contents.
3. Same question for how "reports/{$action}.tpl" resolves for $t_form->fetch() — is there an equivalent override/resolver, or is it a direct literal file path? This matters for figuring out where a=spotlight_direct actually renders from, given the literal file doesn't exist.

THEORY 2 — a shared/global header script or include runs independent of these templates.
4. Search this repo for any JavaScript or PHP include that renders a header/nav and is loaded platform-wide across LEAF apps (report.php pages included) — NOT scoped to whether $_GET['iframe'] is set. Look for something analogous to leaf_header.js (used on the LEAF Launchpad) but potentially also injected into LEAF_Request_Portal pages via a shared layout, a common footer/header include across all LEAF_* apps, or a global <script src="..."> tag that isn't part of main.tpl/main_iframe.tpl at all (e.g. injected via a top-level layout wrapper, a base template all apps extend, or a site-wide analytics/nav script).
5. If found, check whether that script/include has any awareness of iframe context (checks window.self !== window.top, a data attribute, a cookie, etc.) or whether it unconditionally renders regardless of embedding context — that would fully explain the leak independent of report.php's own iframe branching.

Report back what you find for both theories with file paths and line numbers. Still no edits — this is purely to locate the actual source of the rendered header before proposing any fix.